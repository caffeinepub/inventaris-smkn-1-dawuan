{
  "frontend/src/App.tsx": "Main application entry point that sets up TanStack Router with all routes (login, admin, user), wraps the app with AuthProvider, seeds localStorage data on mount, and renders the Toaster for notifications.",
  "frontend/src/pages/admin/BorrowingList.tsx": "Admin borrowing management page showing all borrowings in a filterable paginated table with approve, reject (with reason dialog), and return actions that update item availability in localStorage.",
  "frontend/src/pages/admin/Reports.tsx": "Admin reports page with date range filtering for borrowings, summary statistics cards (total/pending/approved/returned/rejected), a results table, and CSV export functionality.",
  "frontend/src/pages/admin/Settings.tsx": "Admin settings page with a new \"Logo Sekolah\" section featuring file upload (PNG/JPG/SVG, max 2MB), live preview, and remove button; logo is stored as base64 in the Settings object and persisted to localStorage on save.",
  "frontend/src/pages/user/Dashboard.tsx": "User dashboard showing personalized greeting, quick stats (active/pending/total borrowings), currently borrowed items list, and recent borrowing history with status badges.",
  "frontend/src/pages/user/Catalog.tsx": "User item catalog page displaying all inventory items in a responsive card grid with search, category filter, availability badges, and a Borrow button that navigates to the borrow form with pre-filled itemId.",
  "frontend/src/pages/user/BorrowForm.tsx": "User borrowing form page with item selector (available items only), quantity validation against stock, date pickers with validation, purpose textarea, and Pending status submission to localStorage.",
  "frontend/src/pages/user/BorrowingHistory.tsx": "User borrowing history page showing all of the current user's borrowings in a paginated, filterable table with status badges and rejection reason display.",
  "frontend/src/pages/user/Profile.tsx": "User profile page allowing editing of full name, class/position, ID number, and password (with confirmation), while username and role are read-only; includes the app footer with caffeine.ai attribution.",
  "frontend/src/hooks/useQueries.ts": "Minimal placeholder file since this app uses localStorage directly via storage utilities rather than backend canister calls.",
  "frontend/src/pages/admin/ItemList.tsx": "Admin item list page with paginated table, search/category filters, and action buttons (view, edit, delete) — navigation to dynamic routes now uses `useNavigate` with `params` instead of template literal `<Link to>` to satisfy TanStack Router's strict type checking.",
  "frontend/src/pages/admin/ItemDetail.tsx": "Admin item detail page showing all item fields in a read-only card layout — edit navigation now uses `navigate({ to: '/admin/items/$id/edit', params: { id } })` and back navigation uses `navigate()` instead of `<Link>` with template literals.",
  "frontend/src/pages/admin/UserList.tsx": "Admin user list page with paginated table, search filter, and edit/delete actions — edit navigation now uses `useNavigate` with `params: { id: u.id }` on the static route pattern to fix TanStack Router type errors.",
  "frontend/src/types/index.ts": "TypeScript type definitions for all app entities (Item, User, Borrowing, Settings, AuthUser), with `schoolLogo?: string` added to the Settings interface.",
  "frontend/src/utils/storage.ts": "localStorage utility functions for CRUD operations on Items, Users, Borrowings, Settings, and Auth; default Settings now includes `schoolLogo: undefined`.",
  "frontend/src/utils/seedData.ts": "Seeds localStorage with sample items, users, borrowings, and default settings on first load; default settings seed now includes `schoolLogo: undefined`.",
  "frontend/src/components/Layout.tsx": "App layout with sidebar and top header; now reads `schoolLogo` from localStorage settings on mount and on route change, displaying the custom logo image in the sidebar header and mobile top bar when set, falling back to the default asset/icon otherwise.",
  "frontend/src/pages/Login.tsx": "Login page that reads `schoolLogo` from localStorage settings on mount and displays it in the header logo area above the login form; falls back to the static asset image and then a School icon if no custom logo is set. Authentication logic and form layout remain unchanged."
}